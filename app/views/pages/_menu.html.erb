<!-- Don't show links if you're at home or login page -->
<% unless params[:controller] == "pages" || params[:controller] == "devise/sessions" %>
	<%= link_to 'Όλα τα ραντεβού', appointments_path %>
	<%= link_to 'Όλοι οι ασθενείς', clients_path %>
<% end %>

<!-- Show logout link if user is logged in -->
<%= link_to 'Αποσύνδεση', destroy_user_session_path, method: :delete if user_signed_in?%>
